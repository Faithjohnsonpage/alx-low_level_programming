#include "main.h"

/**
 * puts2 - prints every other character of a string
 * @str: input
 *
 * Return: print
 */

void puts2(char *str)
{
	int i;

	while (*str != '\0')
	{
		for (i = 0; i > 0; i += 2)
		{
			_putchar(*s);
		}
		_putchar('\n');
	}
}
